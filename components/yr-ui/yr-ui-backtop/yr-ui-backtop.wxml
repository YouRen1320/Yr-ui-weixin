<view class="yr-ui-backtop__wrap"
		style="width:{{width}}rpx;height:{{width}}rpx;border-radius:50%;bottom: {{bottom}}rpx;right:{{right}}rpx;background:{{background}}"
		wx:if="{{isShow && visible}}" catchtap="goBacktop">
    <yr-ui-icon name="{{name}}" size="52" color="{{color}}" wx:if="{{!custom}}"></yr-ui-icon>
		<slot></slot>
	</view>